/**
 * @file Defines the Enemy tag component for the ECS.
 */
import { defineComponent } from "bitecs";

/**
 * @description A tag component to identify an entity as an enemy.
 */
export const Enemy = defineComponent();
