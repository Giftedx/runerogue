import Phaser from "phaser";

/**
 * UIScene handles the user interface (health/prayer bars, overlays, etc.).
 */
export class UIScene extends Phaser.Scene {
  constructor() {
    super({ key: "UIScene" });
  }

  create(): void {
    // TODO: Implement UI elements (health/prayer bars, etc.)
  }
}
